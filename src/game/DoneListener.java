package game;

/** A done listener is an object that can listen on other objects for when they are done. Then their whenDone method is called. */
public interface DoneListener
{
    void whenDone();
}
